DB_SERVER=host.docker.internal
DB_PORT=3306
DB_NAME=testdb
DB_USER=root
DB_PASSWORD=Passw0rd123456

# Server
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=development

# Security: comma-separated allowed origins, or * for all (dev only)
ALLOWED_ORIGINS=*
